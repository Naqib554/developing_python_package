from storage.addition import add_numbers
res=add_numbers(2,3)
print(res)

# the python file/script in which you import the module from a package that file 
# must be out from the package directory otherwise doesn't work.
